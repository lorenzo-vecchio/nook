version: 2
project_name: nook

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md

brews:
  - repository:
      owner: lorenzo-vecchio
      name: nook
    folder: Formula
    homepage: "https://github.com/lorenzo-vecchio/nook"
    description: "Workspace organizer CLI for developers"

scoop:
  - repository:
      owner: lorenzo-vecchio
      name: nook
    homepage: "https://github.com/lorenzo-vecchio/nook"
    description: "Workspace organizer CLI for developers"

checksum:
  name_template: "checksums.txt"
